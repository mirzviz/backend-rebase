{
  "name": "large-scale-dedup",
  "version": "1.0.0",
  "description": "Dedup a huge line-oriented file under a tight RAM budget using hash-bucket partitioning",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
